@{
  Layout = "_Layout";
}

@using TheArt.Models


<h1>Artists</h1>

@if(@Model.Count() == 0)
{
  <h3>there are no artists</h3>
}
else
{
  <ul>
    @foreach(Artist artist in Model)
    {
      <li>@Html.ActionLink($"{artist.Name}", "Details", new { id = artist.ArtistId })</li>
    }
  </ul>
}

<p>@Html.ActionLink("Home", "Index", "Home")</p>